import React from "react";
import { useLocation } from "react-router-dom";

export const GamePage: React.FC = () => {
//   const location = useLocation();
//   const params = new URLSearchParams(location.search); // Parse query params from URL
//   const mode = params.get("mode"); // Get the selected mode from query params
//   const difficulty = params.get("difficulty"); // Get the difficulty from query params

  return (
    <div>
      {/* <h1>Game Mode: {mode}</h1>
      {difficulty && <h2>Difficulty: {difficulty}</h2>} */}
      {/* Render the game content here based on mode and difficulty */}
    </div>
  );
};
