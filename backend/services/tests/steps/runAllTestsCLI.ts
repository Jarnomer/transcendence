export async function runAllTestsCLI(userCount: number, tournamentCount: number, delay: number) {
  console.log(`\n[🧪] Running all tests...`);
  console.log(`\n[🧪] Creating ${userCount} users...`);
  console.log(`\n[🧪] Creating ${tournamentCount} tournaments...`);
  console.log(`\n[🧪] Delay ${delay}`);
  console.log(`\n[✅] All tests completed successfully.`);
}
